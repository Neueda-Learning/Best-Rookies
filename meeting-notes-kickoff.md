# 初始用户需求会议记录（项目启动）

## 会议信息

| 项目 | 内容                                             |
|------|------------------------------------------------|
| **会议名称** | Portfolio Manager 项目启动需求会议                     |
| **日期** | 2026年7月27日                                     |
| **时间** | 09:30 – 10:30                                  |
| **地点** | 线下                                             |
| **客户方** | 讲师                                             |
| **开发团队** | 成员 A（后端组合）、成员 B（后端持仓/API质量）、成员 C（前端）、成员 D（数据库） |
| **记录人** | 成员 B                                           |

---

## 议程

1. 项目背景介绍（10 分钟）
2. 核心需求说明（20 分钟）
3. 技术方向确认（10 分钟）
4. 数据模型初步讨论（10 分钟）
5. 工作分工与计划（10 分钟）

---

## 会议内容

### 1. 项目背景介绍

**讲师说明项目目标：**

> "这是一个三天的小冲刺项目，目标是让大家把这周学到的 Spring Boot + Vue + MySQL 技术组合起来，做一个真实可用的东西。不需要做得很复杂，但必须能跑起来，最重要的是能演示。"

> "你们要做的是一个投资组合管理工具。用户可以记录自己买了哪些资产——股票、债券、现金都行——然后能看到组合的整体情况。"

**团队成员 A 提问：**
> "需要做用户登录吗？"

**讲师回答：**
> "不需要，假设单用户就好，不用管认证这块，省时间做功能。"

---

### 2. 核心需求说明

讲师口述了以下最基础需求（团队同步记录为用户故事草稿）：

#### 必须有的功能（Must Have）

| 编号 | 需求描述 | 对应接口（初步) |
|------|---------|---------------|
| M-01 | 用户可以查看当前所有持仓 | `GET /api/portfolio` |
| M-02 | 用户可以新增一条持仓记录 | `POST /api/portfolio` |
| M-03 | 用户可以删除一条持仓记录 | `DELETE /api/portfolio/{id}` |
| M-04 | 用户可以看到组合的总成本和总市值 | `GET /api/portfolio/summary` |
| M-05 | 数据必须持久化，重启不丢失 | MySQL 存储 |

**讲师补充：**
> "持仓记录至少要有：股票代码（ticker）、资产类型（股票/债券/现金）、数量、买入均价、创建时间。这是最小集合，你们可以加字段，但不要一开始就搞得很复杂。"

#### 应该有的功能（Should Have）

| 编号 | 需求描述 |
|------|---------|
| S-01 | 有可以交互的 API 文档（Swagger），方便测试 |
| S-02 | 所有错误返回统一 JSON 格式，前端好处理 |
| S-03 | 有图表展示资产分布（饼图或柱状图都行） |

**讲师说：**
> "Swagger 是个好习惯，如果时间够就加上。图表如果来得及做一个就很加分。"

#### 可以做的功能（Could Have）

| 编号 | 需求描述 |
|------|---------|
| C-01 | 对接外部行情 API，展示接近实时的市值 |
| C-02 | 核心流程有自动化测试 |
| C-03 | 有清晰的 README 说明怎么启动 |

**讲师提供行情 API 地址：**
> `https://c4rm9elh30.execute-api.us-east-1.amazonaws.com/default/cachedPriceData?ticker=TSLA`
> "支持的 ticker 有：C、AMZN、TSLA、FB、AAPL。调不到的时候就用买入价代替，别报错。"

---

### 3. 技术方向确认

**讲师确认技术栈：**
- 后端：Spring Boot 3 + JPA + MySQL
- 前端：Vue 3（框架不限，用你们学过的）
- 数据库：MySQL，使用 Flyway 做迁移管理
- API 文档：如果做 Swagger，用 `springdoc-openapi`

**团队成员 B 提问：**
> "前端跨域怎么处理？"

**讲师回答：**
> "后端加 CORS 配置就行，开发阶段直接放行 localhost 就够了。"

**团队成员 C 提问：**
> "前端用 Vite 可以吗？"

**讲师回答：**
> "可以，用 Vite + Vue 3 就行，主流方案。"

---

### 4. 数据模型初步讨论

团队与讲师一起讨论了最小数据模型：

**持仓表（position）初步字段：**

| 字段名 | 类型 | 说明 |
|-------|------|------|
| `id` | BIGINT | 主键，自增 |
| `ticker` | VARCHAR(20) | 股票代码，如 AAPL |
| `assetType` | VARCHAR(20) | 资产类型：STOCK / BOND / CASH |
| `quantity` | DECIMAL | 持有数量 |
| `avgBuyPrice` | DECIMAL | 平均买入价 |
| `createdAt` | TIMESTAMP | 创建时间 |

**讲师说：**
> "先就这几个字段，够用了。不要一开始就建十几个字段，到时候改起来很麻烦。先跑起来，再迭代。"

**成员 D 提问：**
> "需要单独的 portfolio 表吗，还是 position 就够？"

**讲师回答：**
> "加一个 portfolio 表比较合理，一个 portfolio 下面可以有多个 position。这样扩展性也更好。portfolio 就存个名字和基础信息就行。"

**最终确认的数据模型方向：**
```
portfolio (id, name, baseCurrency, createdAt)
    └── position (id, portfolio_id, ticker, assetType, quantity, avgBuyPrice, createdAt)
```

---

### 5. 工作分工与计划

团队内部讨论后，确认以下分工：

| 成员 | 职责 |
|------|------|
| 成员 A | 后端 - Portfolio 相关 CRUD + 汇总接口 |
| 成员 B | 后端 - Position 相关 CRUD + 错误处理 + Swagger |
| 成员 C | 前端 - Vue 3 页面（列表、详情、表单） |
| 成员 D | 数据库 - MySQL 表设计 + Flyway 迁移脚本 |

**三天时间规划（初步）：**

| 天 | 目标 |
|----|------|
| Day 1（今天） | 数据库建好 + 后端骨架跑通 + 前端 Vite 项目初始化 |
| Day 2 | 前后端接口联调 + CRUD 完整跑通 + 汇总接口 |
| Day 3 | Swagger + 图表 + 行情集成 + 演示准备 |

**讲师补充建议：**
> "Day 1 结束前必须能 `POST /api/portfolio` 然后 `GET /api/portfolio` 拿回来数据，这是最低底线。用 Postman 或者 curl 测都行，不需要前端，但必须跑通。"

---

## 决策记录

| 编号 | 决策内容 |
|------|---------|
| D-01 | 不做用户认证，单用户假设 |
| D-02 | 数据模型：portfolio 一对多 position |
| D-03 | 后端端口：8080，前端端口：5173（Vite 默认） |
| D-04 | CORS：开发阶段后端放行 localhost:5173 |
| D-05 | 行情不可用时回退到 avgBuyPrice，不抛错 |
| D-06 | Flyway 管理数据库迁移 |
| D-07 | 先做 Must Have，时间够再做 Should Have |

---

## 开放问题（待跟进）

| 问题 | 状态 | 负责人 |
|------|------|-------|
| assetType 是否需要枚举约束，还是自由文本？ | ⏳ 待确认 | 成员 D |
| summary 接口里市值计算公式是否需要讲师确认？ | ⏳ 待确认 | 成员 A |
| 前端图表库选哪个（ECharts / Chart.js）？ | ⏳ 待确认 | 成员 C |

---

## 下次沟通安排

- **形式**：非正式 check-in（随时 Ping 讲师）
- **正式下次会议**：2026年7月28日 10:00，展示 Day 3 进展，讲师验收核心功能

---

*记录人：成员 D | 审阅：成员 A | 版本：v1.0*
