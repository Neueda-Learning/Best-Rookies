spring:
  datasource:
    url: "${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/portfolio_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}"
    username: "${SPRING_DATASOURCE_USERNAME:root}"
    password: "${SPRING_DATASOURCE_PASSWORD:n3u3da!}"
  jpa:
    hibernate:
      # 验证 DDL（需要数据库已存在）
      ddl-auto: validate
    open-in-view: false
  flyway:
    # 启用 Flyway 数据库迁移
    enabled: true
    # 关闭严格校验以跳过校验码不匹配错误
    validateOnMigrate: false

server:
  port: 8080

