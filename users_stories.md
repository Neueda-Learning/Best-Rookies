# Portfolio Manager - 用户故事

## 背景
- 团队规模：4人
- 周期：3天（敏捷小冲刺）
- 技术栈：Spring Boot + Vue 3 + MySQL
- 需求来源：`portfolio_manager.md`

## 优先级模型（MoSCoW）
- **M — Must Have**：必须完成，没有这个系统无法运作
- **S — Should Have**：应该完成，重要但不阻断核心功能
- **C — Could Have**：可以完成，有时间锦上添花
- **W — Won't Have (this time)**：本次冲刺不做，留待后续迭代

---

## Must Have（必须完成）

### US-001 浏览投资组合持仓
**作为** 用户  
**我希望** 查看投资组合中的全部持仓列表  
**以便** 了解当前所有仓位情况。

**验收标准**
- 后端提供 `GET /api/portfolio`。
- 响应包含：`id`、`ticker`、`assetType`、`quantity`、`avgBuyPrice`、`createdAt`。
- 前端表格正确展示持仓列表。
- 无数据时展示空状态提示。

---

### US-002 新增持仓
**作为** 用户  
**我希望** 新增一条持仓记录  
**以便** 跟踪新投入的资产。

**验收标准**
- 后端提供 `POST /api/portfolio`。
- 必填字段校验：`ticker`、`assetType`、`quantity`、`avgBuyPrice`。
- 新增成功后，前端列表无整页刷新即可更新。
- 输入非法时返回清晰错误提示（字段+原因）。

---

### US-003 删除持仓
**作为** 用户  
**我希望** 删除一条持仓记录  
**以便** 保持组合数据准确。

**验收标准**
- 后端提供 `DELETE /api/portfolio/{id}`。
- 前端每行有删除按钮，点击后弹出确认。
- 删除后该记录从列表消失，无需整页刷新。
- 删除不存在的 `id` 返回 404 及错误说明。

---

### US-004 查看组合表现汇总
**作为** 用户  
**我希望** 查看投资组合的基础表现数据  
**以便** 快速判断整体盈亏情况。

**验收标准**
- 后端提供 `GET /api/portfolio/summary`。
- 返回：总成本（`totalCost`）、总市值（`marketValue`）、未实现盈亏（`unrealizedPnL`）。
- 无行情时以 `avgBuyPrice` 作为当前价格回退（不能返回错误）。
- 前端展示汇总卡片（总成本、总市值、盈亏）。

---

### US-005 数据持久化
**作为** 用户  
**我希望** 持仓数据能持久保存  
**以便** 重启服务后数据不丢失。

**验收标准**
- 数据存储在 MySQL。
- 重启 Spring Boot 服务后，数据仍然存在。

---

## Should Have（应该完成）

### US-006 资产分布图表
**作为** 用户  
**我希望** 以图表方式查看各资产类别占比  
**以便** 直观评估组合多样性。

**验收标准**
- 前端展示至少一张图表（饼图或柱状图均可）。
- 图表数据来自 `GET /api/portfolio/summary`。
- 图表随持仓更新自动刷新。

---

### US-007 API 文档
**作为** 开发者/讲师  
**我希望** 看到可交互的 API 文档  
**以便** 快速测试和评审接口。

**验收标准**
- 后端启用 Swagger UI（访问 `/swagger-ui.html`）。
- 所有接口均有参数说明和示例响应。

---

### US-008 统一错误处理
**作为** 前端开发者  
**我希望** 后端返回统一的 JSON 错误格式  
**以便** 前端可以统一处理并展示友好提示。

**验收标准**
- 所有异常返回结构：`{ timestamp, status, error, message }`。
- 前端对 4xx/5xx 均有友好提示，不显示白屏。

---

## Could Have（可以完成）

### US-009 行情价格集成
**作为** 用户  
**我希望** 查看接近实时的持仓市值  
**以便** 组合表现数据更贴近市场。

**验收标准**
- 后端可调用示例价格 API（`https://c4rm9elh30.execute-api.us-east-1.amazonaws.com/default/cachedPriceData?ticker=TSLA`）。
- 支持 ticker：`C, AMZN, TSLA, FB, AAPL`。
- 当价格 API 调用失败时，自动回退到 `avgBuyPrice`，不报错。

---

### US-010 基础自动化测试
**作为** 团队成员  
**我希望** 核心 API 流程有冒烟测试  
**以便** 降低演示当天故障风险。

**验收标准**
- 覆盖：获取列表、新增、删除、汇总 4 个接口。
- 本地执行 `mvn test` 全部通过。

---

### US-011 README 运行文档
**作为** 讲师/评审者  
**我希望** 有清晰的运行说明  
**以便** 能在5分钟内启动整个系统。

**验收标准**
- `README.md` 包含：环境要求、数据库配置、启动命令、接口示例。

---

## Won't Have（本次不做）

### US-012 用户认证与权限
- 文档明确说明：单用户，无需认证。

### US-013 多用户投资组合管理
- 超出本次冲刺范围，留待后续迭代。

### US-014 历史交易记录与审计日志
- 数据模型复杂度过高，3天内无法稳妥完成。

### US-015 AI/Quantum 功能实现
- 作为探索性研究展示，不作为可交付功能。
- 可在演示中补充一段研究说明文档（1页即可）。
